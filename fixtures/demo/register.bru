meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/register
  body: json
  auth: none
}

body:json {
  {
    "email": "developer@example.com"
  }
}

tests {
  test("returns 200 after successful registration", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
